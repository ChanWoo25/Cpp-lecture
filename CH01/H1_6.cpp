#include <iostream>
using namespace std;

class MyClass {
public:
	int _variables;
	int variables_;
	int m_variables; //등등 관습이 남아있다.
};

int main6()
{
	//reserved keywards [예약어] 변수명으로 지정할 수 없는 이름들이 있다.
	// 변수명으로 대문자만을 쓰지는 않는다. -> 매크로를 지정할 때 관습적으로 쓰임.
	// 전에는 변수를 다 외움 -> 자동완성 기능으로 인해 편해짐.

	return 0;
}